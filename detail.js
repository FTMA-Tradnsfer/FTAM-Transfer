const SUPABASE_URL='https://iloanplyuatfcwzovbpb.supabase.co';const SUPABASE_KEY='sb_publishable_oPXhOaLIGK05Ehw-o6jDsw_TKJODpjM';const db=window.supabase.createClient(SUPABASE_URL,SUPABASE_KEY);const esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[m]));const money=v=>`€${Number(v||0).toFixed(1)}M`;const imageBucket='news-images';const validImage=f=>f&&f.type?.startsWith('image/')&&f.size<=8*1024*1024;async function uploadPlayerImage(file){if(!validImage(file))throw new Error('이미지는 JPG, PNG, WEBP, GIF 형식의 8MB 이하 파일만 사용할 수 있습니다.');const ext=(file.name.split('.').pop()||'jpg').toLowerCase().replace(/[^a-z0-9]/g,'')||'jpg';const path=`players/${Date.now()}-${crypto.randomUUID()}.${ext}`;const {error}=await db.storage.from(imageBucket).upload(path,file,{contentType:file.type,upsert:false,cacheControl:'31536000'});if(error)throw error;return db.storage.from(imageBucket).getPublicUrl(path).data.publicUrl}function adminAuthorized(){return sessionStorage.getItem('ftma_admin_ok')==='1'}function requireAdmin(){if(adminAuthorized())return true;const entered=prompt('관리자 비밀번호를 입력하세요.');if(entered==='ruru2622##'){sessionStorage.setItem('ftma_admin_ok','1');return true}alert('관리자 권한이 없습니다.');return false}function bindSquadUI(clubId){const open=document.getElementById('openAddPlayer'),panel=document.getElementById('clubAddPanel'),cancel=document.getElementById('cancelAdd'),form=document.getElementById('clubAddForm'),file=document.getElementById('squadPlayerImage'),preview=document.getElementById('squadImagePreview'),message=document.getElementById('clubAddMessage');open?.addEventListener('click',()=>{if(!requireAdmin())return;panel.classList.toggle('open')});cancel?.addEventListener('click',()=>{panel.classList.remove('open');form.reset();preview.style.display='none';message.textContent='' });file?.addEventListener('change',()=>{const f=file.files?.[0];if(!f){preview.style.display='none';return}if(!validImage(f)){file.value='';preview.style.display='none';message.textContent='이미지는 8MB 이하의 JPG/PNG/WEBP/GIF만 가능합니다.';return}preview.src=URL.createObjectURL(f);preview.style.display='block';message.textContent=''});form?.addEventListener('submit',async e=>{e.preventDefault();if(!requireAdmin())return;const fd=new FormData(form);const submit=form.querySelector('button.save');submit.disabled=true;message.textContent='선수 등록 중...';try{let photo_url=null;const file=fd.get('photo');if(file instanceof File&&file.size)photo_url=await uploadPlayerImage(file);const payload={name:String(fd.get('name')||'').trim(),position:String(fd.get('position')||'').trim()||null,nationality:String(fd.get('nationality')||'').trim()||null,current_club_id:clubId,photo_url,shirt_number:fd.get('shirt_number')?Number(fd.get('shirt_number')):null,squad_type:String(fd.get('squad_type')||'first_team')};if(!payload.name)throw new Error('선수 이름을 입력해주세요.');const {error}=await db.from('players').insert(payload);if(error)throw error;message.textContent='선수가 선수단에 추가되었습니다.';form.reset();preview.style.display='none';await renderClub(clubId)}catch(err){console.error(err);message.textContent=`등록 실패: ${err.message||err}`}finally{submit.disabled=false}})}async function renderClub(id){const {data:c,error}=await db.from('clubs').select('id,name,country,league,description,logo_url').eq('id',id).single();if(error)throw error;const {data:players,error:playerError}=await db.from('players').select('id,name,photo_url,nationality,position,ability,potential,market_value,shirt_number,squad_type').eq('current_club_id',id).order('squad_type').order('shirt_number',{ascending:true,nullsFirst:false}).order('name');if(playerError)throw playerError;document.title=`FTMA | ${c.name}`;document.getElementById('clubName').textContent=c.name;document.getElementById('clubIntro').textContent=`${c.country||'국가 미상'} · ${c.league||'리그 미상'}`;const first=players?.filter(p=>p.squad_type!=='u20')||[],u20=players?.filter(p=>p.squad_type==='u20')||[];const squad=(list,empty)=>list.length?list.map(p=>`<a class="squad-player" href="player.html?id=${encodeURIComponent(p.id)}">${p.photo_url?`<img src="${esc(p.photo_url)}" alt="${esc(p.name)}">`:''}<div><b>${esc(p.name)}</b><span>${esc(p.position||'포지션 미정')} · 어빌 ${p.ability??'—'} · ${money(p.market_value)}</span></div>${p.shirt_number?`<span class="shirt">#${p.shirt_number}</span>`:''}</a>`).join(''):empty;document.getElementById('clubDetail').innerHTML=`<div class="detail-card"><p class="eyebrow">CLUB PROFILE</p><div class="detail-value">${players?.length||0}</div><p>현재 소속 선수</p><p>${esc(c.description||'등록된 구단 설명이 없습니다.')}</p></div><div class="detail-card"><h2>구단 정보</h2><div class="detail-list"><div><span>국가</span><b>${esc(c.country||'—')}</b></div><div><span>리그</span><b>${esc(c.league||'—')}</b></div></div></div><div class="detail-card detail-wide"><h2>현재 선수단</h2><p class="squad-group-title">FIRST TEAM · ${first.length}명</p><div class="squad-list">${squad(first,'등록된 1군 선수가 없습니다.')}</div><p class="squad-group-title">U20 · ${u20.length}명</p><div class="squad-list">${squad(u20,'등록된 U20 선수가 없습니다.')}</div></div>`}async function boot(){const id=new URLSearchParams(location.search).get('id');if(!id)return;const isPlayer=location.pathname.endsWith('player.html');try{if(isPlayer){const {data:p,error}=await db.from('players').select('id,name,photo_url,nationality,position,ability,potential,market_value,status,current_club_id,age,birth_date,shirt_number,squad_type,clubs(id,name,country,league)').eq('id',id).single();if(error)throw error;document.title=`FTMA | ${p.name}`;document.getElementById('playerName').textContent=p.name;document.getElementById('playerIntro').textContent=`${p.nationality||'국적 미상'} · ${p.position||'포지션 미정'} · ${p.clubs?.name||'미소속'}`;document.getElementById('playerDetail').innerHTML=`${p.photo_url?`<div class="detail-card detail-wide"><img class="player-profile-image" src="${esc(p.photo_url)}" alt="${esc(p.name)}"></div>`:''}<div class="detail-card"><p class="eyebrow">PLAYER DATA</p><div class="detail-value">${money(p.market_value)}</div><p>현재 시장가치</p></div><div class="detail-card"><h2>기본 정보</h2><div class="detail-list"><div><span>포지션</span><b>${esc(p.position||'미정')}</b></div><div><span>국적</span><b>${esc(p.nationality||'미상')}</b></div><div><span>나이</span><b>${p.age??'—'}</b></div><div><span>생년월일</span><b>${esc(p.birth_date||'—')}</b></div><div><span>소속 구단</span><b>${esc(p.clubs?.name||'미소속')}</b></div><div><span>선수단</span><b>${p.squad_type==='u20'?'U20':'1군'}</b></div><div><span>등번호</span><b>${p.shirt_number?`#${p.shirt_number}`:'—'}</b></div><div><span>상태</span><b>${esc(p.status||'관리')}</b></div></div></div><div class="detail-card"><h2>능력치</h2><div class="detail-list"><div><span>어빌</span><b>${p.ability??'—'}</b></div><div><span>포텐</span><b>${p.potential??'—'}</b></div></div></div>`}else{bindSquadUI(id);await renderClub(id)}}catch(e){console.error(e);const box=document.getElementById(isPlayer?'playerDetail':'clubDetail');if(box)box.innerHTML=`<div class="loading">정보를 불러오지 못했습니다: ${esc(e.message)}</div>`}}boot();