const SUPABASE_URL='https://iloanplyuatfcwzovbpb.supabase.co';const SUPABASE_KEY='sb_publishable_oPXhOaLIGK05Ehw-o6jDsw_TKJODpjM';const db=window.supabase.createClient(SUPABASE_URL,SUPABASE_KEY);const esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[m]));const money=v=>`€${Number(v||0).toFixed(1)}M`;async function boot(){const id=new URLSearchParams(location.search).get('id');if(!id)return;const isPlayer=location.pathname.endsWith('player.html');try{if(isPlayer){const {data:p,error}=await db.from('players').select('id,name,nationality,position,ability,potential,market_value,status,current_club_id,age,birth_date,clubs(id,name,country,league)').eq('id',id).single();if(error)throw error;document.title=`FTMA | ${p.name}`;document.getElementById('playerName').textContent=p.name;document.getElementById('playerIntro').textContent=`${p.nationality||'국적 미상'} · ${p.position||'포지션 미정'} · ${p.clubs?.name||'미소속'}`;document.getElementById('playerDetail').innerHTML=`<div class="detail-card"><p class="eyebrow">PLAYER DATA</p><div class="detail-value">${money(p.market_value)}</div><p>현재 시장가치</p></div><div class="detail-card"><h2>기본 정보</h2><div class="detail-list"><div><span>포지션</span><b>${esc(p.position||'미정')}</b></div><div><span>국적</span><b>${esc(p.nationality||'미상')}</b></div><div><span>나이</span><b>${p.age??'—'}</b></div><div><span>생년월일</span><b>${esc(p.birth_date||'—')}</b></div><div><span>소속 구단</span><b>${esc(p.clubs?.name||'미소속')}</b></div><div><span>상태</span><b>${esc(p.status||'관리')}</b></div></div></div><div class="detail-card"><h2>능력치</h2><div class="detail-list"><div><span>어빌</span><b>${p.ability??'—'}</b></div><div><span>포텐</span><b>${p.potential??'—'}</b></div></div></div>`}else{const {data:c,error}=await db.from('clubs').select('id,name,country,league,description,logo_url').eq('id',id).single();if(error)throw error;const {data:players}=await db.from('players').select('id,name,nationality,position,ability,potential,market_value').eq('current_club_id',id).order('market_value',{ascending:false});document.title=`FTMA | ${c.name}`;document.getElementById('clubName').textContent=c.name;document.getElementById('clubIntro').textContent=`${c.country||'국가 미상'} · ${c.league||'리그 미상'}`;document.getElementById('clubDetail').innerHTML=`<div class="detail-card"><p class="eyebrow">CLUB PROFILE</p><div class="detail-value">${players?.length||0}</div><p>현재 소속 선수</p><p>${esc(c.description||'등록된 구단 설명이 없습니다.')}</p></div><div class="detail-card"><h2>구단 정보</h2><div class="detail-list"><div><span>국가</span><b>${esc(c.country||'—')}</b></div><div><span>리그</span><b>${esc(c.league||'—')}</b></div></div></div><div class="detail-card detail-wide"><h2>현재 선수단</h2><div class="squad-list">${players?.length?players.map(p=>`<a href="player.html?id=${encodeURIComponent(p.id)}"><b>${esc(p.name)}</b><span>${esc(p.position||'—')} · 어빌 ${p.ability??'—'} · ${money(p.market_value)}</span></a>`).join(''):'<div class="loading">등록된 선수가 없습니다.</div>'}</div></div>`}}catch(e){const box=document.getElementById(isPlayer?'playerDetail':'clubDetail');box.innerHTML=`<div class="loading">정보를 불러오지 못했습니다: ${esc(e.message)}</div>`}}boot();