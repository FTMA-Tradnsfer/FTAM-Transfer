(function(){const U='https://iloanplyuatfcwzovbpb.supabase.co',K='sb_publishable_oPXhOaLIGK05Ehw-o6jDsw_TKJODpjM';const db=window.supabase.createClient(U,K);const cid=()=>sessionStorage.getItem('ftma_club_id')||localStorage.getItem('ftma_club_id')||'';const tok=()=>sessionStorage.getItem('ftma_club_session_token')||localStorage.getItem('ftma_club_session_token')||'';const esc=s=>String(s??'').replace(/[&<>\"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#039;'}[m]));const money=v=>`€${Number(v||0).toFixed(Number(v)%1?'1':'0')}M`;async function load(){const box=document.getElementById('favorites');if(!box)return;const token=tok();if(!token){box.innerHTML='<p class="favorite-empty">구단 로그인이 필요합니다.</p>';return}const {data,error}=await db.rpc('get_club_favorites',{p_session_token:token});if(error||!data?.success){box.innerHTML=`<p class="favorite-empty">관심 선수 정보를 불러오지 못했습니다.${data?.message?` ${esc(data.message)}`:''}</p>`;return}const favs=data.favorites||[];box.innerHTML=favs.length?favs.map(p=>`<a class="favorite-card" href="player.html?id=${encodeURIComponent(p.player_id)}">${p.photo_url?`<img src="${esc(p.photo_url)}" alt="${esc(p.name)}" loading="lazy">`:'<img alt="" src="">'}<div><b>${esc(p.name)}</b><small>${esc(p.position||'포지션 미정')} · ${esc(p.nationality||'국적 미상')}</small><small>${money(p.market_value)} · ${esc(p.club_name||'미소속')}</small></div></a>`).join(''):'<p class="favorite-empty">관심 선수가 없습니다. 이적시장에서 ☆ 관심 등록으로 저장할 수 있습니다.</p>'}load();})();