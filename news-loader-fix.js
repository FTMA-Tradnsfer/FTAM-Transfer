(()=>{
const U='https://iloanplyuatfcwzovbpb.supabase.co',K='sb_publishable_oPXhOaLIGK05Ehw-o6jDsw_TKJODpjM';
const H={apikey:K,Authorization:`Bearer ${K}`};
const esc=s=>String(s??'').replace(/[&<>\"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#039;'}[m]));
async function getNews(){const r=await fetch(`${U}/rest/v1/news?select=id,title,summary,body,cover_image_url,category,published,published_at&published=eq.true&order=published_at.desc`,{headers:H,cache:'no-store'});if(!r.ok)throw new Error(await r.text());return r.json()}
function renderHome(ns){const hero=document.getElementById('featuredNews'),image=document.getElementById('featuredImage'),side=document.getElementById('newsSideList');if(!hero&&!side)return;if(!ns.length){if(side)side.innerHTML='<div class="news-empty-side">아직 등록된 뉴스가 없습니다.</div>';return}const a=ns[0];if(hero){hero.href=`news.html?id=${encodeURIComponent(a.id)}`;if(image)image.innerHTML=a.cover_image_url?`<img src="${esc(a.cover_image_url)}" alt="${esc(a.title)}">`:'<div class="empty-news">대표 이미지가 없습니다.</div>';const c=document.getElementById('featuredCategory'),t=document.getElementById('featuredTitle'),s=document.getElementById('featuredSummary');if(c)c.textContent=a.category||'FTMA NEWS';if(t)t.textContent=a.title;if(s)s.textContent=a.summary||''}if(side)side.innerHTML=ns.slice(0,5).map((x,i)=>`<a href="news.html?id=${encodeURIComponent(x.id)}" class="news-side-item"><span>${String(i+1).padStart(2,'0')}</span><div><small>${esc(x.category||'FTMA NEWS')}</small><b>${esc(x.title)}</b><em>${x.published_at?new Date(x.published_at).toLocaleDateString('ko-KR'):''}</em></div></a>`).join('')}
function renderNewsPage(ns){const list=document.getElementById('newsList'),count=document.getElementById('newsCount');if(!list)return;if(count)count.textContent=`${ns.length} ARTICLES`;const id=new URLSearchParams(location.search).get('id');if(id){const a=ns.find(x=>String(x.id)===String(id));if(a){list.hidden=true;document.querySelector('.news-page-head')?.setAttribute('hidden','');const view=document.getElementById('articleView');if(view){view.hidden=false;document.getElementById('articleContent').innerHTML=`<div class="article-meta">${esc(a.category||'FTMA NEWS')} · ${a.published_at?new Date(a.published_at).toLocaleDateString('ko-KR'):''}</div><h1>${esc(a.title)}</h1><p class="article-summary">${esc(a.summary||'')}</p>${a.cover_image_url?`<img class="article-cover" src="${esc(a.cover_image_url)}" alt="${esc(a.title)}">`:''}<div class="article-body">${esc(a.body||'').replace(/\n/g,'<br>')}</div>`}return}}if(!ns.length){list.innerHTML='<div class="empty-news-page">등록된 기사가 없습니다.<br><small>관리자 페이지에서 첫 기사를 등록해보세요.</small></div>';return}list.innerHTML=ns.map((a,i)=>`<a class="news-item" href="news.html?id=${encodeURIComponent(a.id)}"><div class="news-thumb">${a.cover_image_url?`<img src="${esc(a.cover_image_url)}" alt="${esc(a.title)}">`:''}</div><div class="news-item-body"><span>${esc(a.category||'FTMA NEWS')} · ${a.published_at?new Date(a.published_at).toLocaleDateString('ko-KR'):''}</span><h2>${esc(a.title)}</h2><p>${esc(a.summary||'')}</p><strong>기사 보기 →</strong></div><b class="news-index">${String(i+1).padStart(2,'0')}</b></a>`).join('')}
async function boot(){try{const ns=await getNews();renderHome(ns);renderNewsPage(ns)}catch(e){console.error('[FTMA news fix]',e);const side=document.getElementById('newsSideList'),list=document.getElementById('newsList');if(side)side.innerHTML='<div class="news-empty-side">뉴스를 불러오지 못했습니다. 잠시 후 다시 시도해주세요.</div>';if(list)list.innerHTML='<div class="loading">뉴스를 불러오지 못했습니다. 잠시 후 다시 시도해주세요.</div>'}}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot);else boot();
})();