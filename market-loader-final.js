/* FTMA market loader — resilient DB fetch + name search compatible. */
(function(){
'use strict';
const URL='https://iloanplyuatfcwzovbpb.supabase.co',KEY='sb_publishable_oPXhOaLIGK05Ehw-o6jDsw_TKJODpjM';
let db=null,loaded=false;
const esc=s=>String(s??'').replace(/[&<>\"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#039;'}[m]));
const money=v=>`€${Number(v||0).toFixed(1)}M`,age=p=>p.age??'—';
const loggedIn=()=>Boolean(sessionStorage.getItem('ftma_club_id')&&sessionStorage.getItem('ftma_club_session_token'));
const timeout=p=>Promise.race([p,new Promise((_,reject)=>setTimeout(()=>reject(new Error('요청 시간 초과')),8000))));
async function rest(path){const r=await timeout(fetch(`${URL}/rest/v1/${path}`,{headers:{apikey:KEY,Authorization:`Bearer ${KEY}`,Accept:'application/json'}}));if(!r.ok)throw new Error(`DB 요청 실패 (${r.status})`);return r.json()}
async function loadPlayers(){const fields='id,name,photo_url,nationality,position,market_value,age,birth_date,shirt_number,current_club_id,is_loan,is_nfs';if(window.supabase?.createClient){let r=await timeout(db.from('players').select(fields).order('market_value',{ascending:false}).limit(2000));if(!r.error)return r.data||[];r=await timeout(db.from('players').select('id,name,photo_url,nationality,position,market_value,age,birth_date,shirt_number,current_club_id').order('market_value',{ascending:false}).limit(2000));if(!r.error)return(r.data||[]).map(p=>({...p,is_loan:false,is_nfs:false}))}return rest(`players?select=${encodeURIComponent(fields)}&order=market_value.desc&limit=2000`)}
async function loadClubs(){if(window.supabase?.createClient){const r=await timeout(db.from('clubs').select('id,name').order('name').limit(2000));if(!r.error)return r.data||[]}return rest('clubs?select=id,name&order=name&limit=2000')}
async function load(){const box=document.getElementById('marketList'),count=document.getElementById('marketCount');if(!box||loaded)return;box.innerHTML='<div class="loading">이적시장 데이터를 불러오는 중입니다...</div>';try{if(window.supabase?.createClient)db=window.supabase.createClient(URL,KEY);const[players,clubs]=await Promise.all([loadPlayers(),loadClubs()]);const clubMap=new Map(clubs.map(c=>[String(c.id),c]));const market=players.filter(p=>!Boolean(p.is_loan)&&!Boolean(p.is_nfs)).map(p=>({...p,clubs:clubMap.get(String(p.current_club_id))||null}));if(count)count.textContent=`${market.length}명`;const canOffer=loggedIn();box.innerHTML=market.length?market.map((p,i)=>{const action=canOffer?`<button type="button" class="market-offer-btn" data-player-id="${esc(p.id)}">이적 제안</button>`:'<em class="status open">시장 등록</em>';return `<div class="market-directory-row"><a class="market-player-cell" href="player.html?id=${encodeURIComponent(p.id)}">${p.photo_url?`<img class="market-player-photo" src="${esc(p.photo_url)}" alt="${esc(p.name)}" loading="lazy" decoding="async">`:''}<span class="player-rank">${String(i+1).padStart(2,'0')}</span><div class="market-player-main"><div class="market-player-name-line"><b>${esc(p.name)}</b></div><small>${esc(p.position||'미정')} · ${esc(p.nationality||'국적 미상')} · ${esc(p.clubs?.name||'미소속')} · ${esc(age(p))}세 · ${p.shirt_number!=null?`#${p.shirt_number}`:'번호 미등록'}</small></div></a><strong>${money(p.market_value)}</strong>${action}</div>`}).join(''):'<div class="loading">현재 이적 가능한 선수가 없습니다.</div>';loaded=true;if(canOffer&&db){try{const s=await timeout(db.rpc('get_transfer_market_state'));const open=!s.error&&Boolean(s.data?.open);if(!open)document.querySelectorAll('.market-offer-btn').forEach(b=>{b.disabled=true;b.classList.add('closed');b.textContent='시장 닫힘'})}catch(_){}}}catch(e){console.error('[FTMA] market loader failed',e);if(count)count.textContent='—';box.innerHTML='<div class="loading">이적시장 DB 연결에 실패했습니다. 잠시 후 새로고침해주세요.</div>'}}
function loadHotfix(){if(document.getElementById('ftmaOfferHotfixScript'))return;const s=document.createElement('script');s.id='ftmaOfferHotfixScript';s.src='transfer-market-offer-hotfix.js?v=20260831hotfix1';document.head.appendChild(s)}
function boot(){if(document.getElementById('marketList')){loadHotfix();load()}}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot();
})();