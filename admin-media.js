window.uploadImage=async function(file,folder){if(!(file instanceof File)||!file.size)throw new Error('이미지 파일을 선택해주세요.');if(!file.type.startsWith('image/'))throw new Error('이미지 파일만 업로드할 수 있습니다.');if(file.size>8*1024*1024)throw new Error('이미지는 8MB 이하로 업로드해주세요.');const token=sessionStorage.getItem('ftma_admin_token');if(!token)throw new Error('관리자 세션이 만료되었습니다. 다시 로그인해주세요.');const form=new FormData();form.append('file',file,file.name);form.append('folder',folder);const r=await fetch('https://iloanplyuatfcwzovbpb.supabase.co/functions/v1/ftma-media-upload',{method:'POST',headers:{'x-ftma-admin-token':token},body:form});const text=await r.text();let data=null;try{data=text?JSON.parse(text):null}catch{}if(!r.ok||!data?.ok)throw new Error(data?.error||`이미지 업로드 실패 (HTTP ${r.status})`);return data.publicUrl};