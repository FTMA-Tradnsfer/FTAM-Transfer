const SUPABASE_URL='https://iloanplyuatfcwzovbpb.supabase.co';const SUPABASE_KEY='sb_publishable_oPXhOaLIGK05Ehw-o6jDsw_TKJODpjM';const db=window.supabase.createClient(SUPABASE_URL,SUPABASE_KEY);let players=[],clubs=[],news=[];const money=v=>`€${Number(v||0).toFixed(1)}M`;const esc=s=>String(s??'').replace(/[&<>\"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#039;'}[m]));async function loadData(){const [{data:p,error:pe},{data:c,error:ce},{data:n,error:ne}]=await Promise.all([db.from('players').select('id,name,nationality,position,ability,potential,market_value,status,current_club_id,age,clubs(name)').order('market_value',{ascending:false}),db.from('clubs').select('id,name,country,league').order('name'),db.from('news').select('id,title,summary,cover_image_url,category,published,published_at').eq('published',true).order('published_at',{ascending:false}).limit(6)]);if(pe||ce||ne){console.error(pe||ce||ne);return showError('FTMA 데이터베이스에 연결할 수 없습니다. 잠시 후 다시 시도해주세요.')}players=p||[];clubs=c||[];news=n||[];render(players,clubs);renderNews(news)}function render(ps,cs){document.getElementById('playerCount').textContent=ps.length;document.getElementById('clubCount').textContent=cs.length;const u20=ps.filter(p=>Number(p.age||99)<20).length;document.getElementById('u20Count').textContent=u20;document.getElementById('dashPlayers').textContent=ps.length;document.getElementById('dashClubs').textContent=cs.length;document.getElementById('dashU20').textContent=u20;const total=ps.reduce((a,p)=>a+Number(p.market_value||0),0);document.getElementById('marketValue').textContent=money(total);document.getElementById('totalValue').textContent=money(total);document.getElementById('marketRows').innerHTML=ps.length?ps.map((p,i)=>`<article class="player-row"><div class="player"><span class="avatar">${String(i+1).padStart(2,'0')}</span><span><b>${esc(p.name)}</b><small>${esc(p.nationality||'미상')} • ${esc(p.age??'유망주')}</small></span></div><span>${esc(p.position||'미정')}</span><span>${esc(p.clubs?.name||'미소속')}</span><strong>${money(p.market_value)}</strong><em class="status ${p.status==='available'?'open':'hot'}">${p.status==='available'?'시장 등록':'기록 관리'}</em></article>`).join(''):'<div class="loading">등록된 선수가 없습니다.</div>';document.getElementById('playerGrid').innerHTML=ps.map(p=>`<article class="profile-card"><div class="rating">${p.ability??'—'}</div><span class="position">${esc(p.position||'미정')}</span><h3>${esc(p.name)}</h3><p>${esc(p.nationality||'미상')} • ${esc(p.clubs?.name||'미소속')}</p><div class="profile-meta"><span>어빌 <b>${p.ability??'—'}</b></span><span>포텐 <b>${p.potential??'—'}</b></span><span>시장가치 <b>${money(p.market_value)}</b></span></div></article>`).join('');document.getElementById('clubGrid').innerHTML=cs.map(c=>`<div class="club-card"><span>${esc((c.name||'FT').slice(0,2).toUpperCase())}</span><b>${esc(c.name)}</b><small>${esc(c.league||'판타지 리그')}</small></div>`).join('')}function renderNews(ns){const hero=document.getElementById('featuredNews'),image=document.getElementById('featuredImage'),side=document.getElementById('newsSideList');if(!hero)return;if(!ns.length){side.innerHTML='<div class="news-empty-side">아직 등록된 뉴스가 없습니다.</div>';return}const a=ns[0];hero.href=`news.html?id=${encodeURIComponent(a.id)}`;image.innerHTML=`<img src="${esc(a.cover_image_url)}" alt="${esc(a.title)}">`;document.getElementById('featuredCategory').textContent=esc(a.category||'FTMA NEWS');document.getElementById('featuredTitle').textContent=esc(a.title);document.getElementById('featuredSummary').textContent=esc(a.summary||'');side.innerHTML=ns.slice(0,5).map((x,i)=>`<a href="news.html?id=${encodeURIComponent(x.id)}" class="news-side-item"><span>${String(i+1).padStart(2,'0')}</span><div><small>${esc(x.category||'FTMA NEWS')}</small><b>${esc(x.title)}</b><em>${new Date(x.published_at).toLocaleDateString('ko-KR')}</em></div></a>`).join('')}function showError(msg){document.getElementById('marketRows').innerHTML=`<div class="loading">${esc(msg)}</div>`}document.getElementById('loginBtn').addEventListener('click',()=>location.href='admin.html');loadData();